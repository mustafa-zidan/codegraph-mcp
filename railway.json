{
  "$schema": "https://railway.com/railway.schema.json",
  "build": {
    "builder": "DOCKERFILE",
    "dockerfilePath": "Dockerfile"
  },
  "deploy": {
    "startCommand": "python -m codegraph_mcp serve --transport sse",
    "healthcheckPath": "/health",
    "restartPolicyType": "ON_FAILURE"
  }
}
