# Changelog

## [0.1.0]

Initial published release: MCP server that builds a code knowledge graph (Tree-sitter parsers, NetworkX, Kuzu storage
with FTS, FastMCP). Optional semantic search via `pip install codegraph-mcp[semantic]` (NumPy vector index + OpenAI
compatible or local `sentence-transformers` embeddings).
